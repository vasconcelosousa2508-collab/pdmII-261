name: avaliacao_3
description: aplicação Dart que acessa um banco de dados com tratamento de exceção.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  sqflite_common_ffi: ^2.3.0
  path: ^1.9.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0